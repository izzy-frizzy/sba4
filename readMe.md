Challenges faced during the project.

*challenges I faced was both putting an object in an array to then return the objects in a string as well as implementing a filter for the tasks and filtering them by the different criteria

How you approached solving those challenges.

*for the first challenge i first initilized the object with the html id's and then pushed the values into an array, at the time it seemed harder than it looks since i haven't done that before. then for the next challenge I made new inputs and a button for the filter system and i made a filter function for the button and i was working on it for a long time because it wasn't working but the problem was a syntax error in my html since the value select options didn't match with the value i was comparing it to 

What you would improve if given more time.

*what I would improve if i had more time is make a css page and add styling to the project since the functionality is all there, it just needs to look nice now